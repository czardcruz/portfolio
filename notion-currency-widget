<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USD to YER Converter</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }
        
        .widget {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            max-width: 400px;
            width: 100%;
        }
        
        h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 10px;
            text-align: center;
        }
        
        .subtitle {
            text-align: center;
            color: #666;
            font-size: 14px;
            margin-bottom: 25px;
        }
        
        .conversion-display {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            color: white;
            text-align: center;
        }
        
        .amount {
            font-size: 48px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .currency-label {
            font-size: 18px;
            opacity: 0.9;
            margin-bottom: 15px;
        }
        
        .equals {
            font-size: 20px;
            margin: 15px 0;
            opacity: 0.8;
        }
        
        .info {
            display: flex;
            justify-content: space-between;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 10px;
            margin-bottom: 15px;
        }
        
        .info-item {
            text-align: center;
        }
        
        .info-label {
            font-size: 12px;
            color: #666;
            margin-bottom: 5px;
        }
        
        .info-value {
            font-size: 14px;
            color: #333;
            font-weight: 600;
        }
        
        .refresh-btn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s, opacity 0.2s;
        }
        
        .refresh-btn:hover {
            transform: translateY(-2px);
            opacity: 0.9;
        }
        
        .refresh-btn:active {
            transform: translateY(0);
        }
        
        .loading {
            text-align: center;
            color: #666;
            padding: 20px;
        }
        
        .error {
            background: #fee;
            color: #c33;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="widget">
        <h1>💱 Currency Converter</h1>
        <p class="subtitle">US Dollar to Yemeni Rial</p>
        
        <div id="content">
            <div class="loading">Loading exchange rate...</div>
        </div>
    </div>

    <script>
        let currentRate = null;
        let lastUpdated = null;

        async function fetchExchangeRate() {
            try {
                const response = await fetch('https://api.exchangerate-api.com/v4/latest/USD');
                const data = await response.json();
                
                currentRate = data.rates.YER;
                lastUpdated = new Date();
                
                displayConversion();
            } catch (error) {
                displayError('Failed to fetch exchange rate. Please try again.');
            }
        }

        function displayConversion() {
            const now = new Date();
            const timeStr = now.toLocaleTimeString('en-US', { 
                hour: '2-digit', 
                minute: '2-digit' 
            });
            const dateStr = now.toLocaleDateString('en-US', { 
                month: 'short', 
                day: 'numeric',
                year: 'numeric'
            });

            document.getElementById('content').innerHTML = `
                <div class="conversion-display">
                    <div class="amount">$1</div>
                    <div class="currency-label">US Dollar</div>
                    <div class="equals">≈</div>
                    <div class="amount">${currentRate.toFixed(2)}</div>
                    <div class="currency-label">Yemeni Rial</div>
                </div>
                
                <div class="info">
                    <div class="info-item">
                        <div class="info-label">Rate</div>
                        <div class="info-value">${currentRate.toFixed(4)}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Updated</div>
                        <div class="info-value">${timeStr}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Date</div>
                        <div class="info-value">${dateStr}</div>
                    </div>
                </div>
                
                <button class="refresh-btn" onclick="fetchExchangeRate()">
                    🔄 Refresh Rate
                </button>
            `;
        }

        function displayError(message) {
            document.getElementById('content').innerHTML = `
                <div class="error">${message}</div>
                <button class="refresh-btn" onclick="fetchExchangeRate()">
                    🔄 Try Again
                </button>
            `;
        }

        // Fetch rate on load
        fetchExchangeRate();
        
        // Auto-refresh every 5 minutes
        setInterval(fetchExchangeRate, 300000);
    </script>
</body>
</html>
